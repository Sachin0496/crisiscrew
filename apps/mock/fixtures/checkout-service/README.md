# checkout-service

Places orders and takes payment through the payment gateway (UPI, cards, netbanking).

```bash
npm test
```

Owned by the Payments team. On call: Freshservice schedule "Payments".
